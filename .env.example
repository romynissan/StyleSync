# PostgreSQL — Supabase, Railway, or local Docker (see docker-compose.yml)
# Encode special characters in passwords (e.g. @ becomes %40)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT.supabase.co:5432/postgres?sslmode=require"
